<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <%- include("partials/header") %>  

    <div class="container mt-5">
        <div class="card">
            <% if (post.image) { %>
                <img src="<%= post.image %>" alt="Post Image" class="img-fluid mx-auto d-block" style="max-width: 100%; height: auto;">
            <% } %>
            <div class="card-body">
                <h1 class="card-title text-center"><%= post.title %></h1>
                <p class="text-muted text-center"><strong>Category:</strong> <%= post.category %></p>
                
                <!-- ✅ Content Preserved with Formatting -->
                <div class="post-content">
                    <%- post.content %> <!-- ✅ Displays content exactly as entered in the admin panel -->
                </div>

                <div class="text-center mt-4">
                    <a href="/" class="btn btn-secondary">Back to Home</a>
                </div>
            </div>
        </div>
    </div>

    <%- include("partials/footer") %>  

    <script src="/js/script.js"></script>
</body>
</html>
